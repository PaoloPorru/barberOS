# Sviluppo: lasciare vuoto — Vite proxy su /api → localhost:3000
#
# OBBLIGATORIO su Vercel + Render free (Environment Variables, spunta Production + Preview):
# VITE_API_BASE_URL=https://tuo-api.onrender.com/api
# (senza questo, il proxy serverless va in 504 durante il cold start Render)
